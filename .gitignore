# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Derived index / local session state — never committed (text is the truth)
mapper.db
*.db
maps/.mapper/
.mapper/

# Exported artifacts (generated on demand)
*.svg
*.png

# IDEs
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Batch scratch — the surface of two mutation-left-on-disk incidents.
# Neither exists in the tree today; the entry is preventive.
scratch/
out.txt

# Credentials — none exist today, and there is to be no first time.
.env
.env.*

# Throwaway UI prototypes. Kept on disk deliberately (batch 2 and 3 build from
# them) and NEVER committed: they are a different technology from the shipping
# target, and decision D13's ruff metric of 29 is only valid while they stay
# untracked -- a bare `ruff check .` reads 57, and the other 28 are all here.
prototypes/
